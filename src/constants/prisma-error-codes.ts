/**
 * Prisma {@link https://www.prisma.io/docs/orm/reference/error-reference#error-codes Client known request error codes} referenced in handlers.
 */

/** Returned when an insert or update violates a unique index (e.g. duplicate email). */
export const UNIQUE_CONSTRAINT_VIOLATION = "P2002";
